{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun --watch index.ts",
    "start": "bun index.ts",
    "build": "bunx tsc",
    "check-types": "bunx tsc --noEmit"
  },
  "dependencies": {
    "kafkajs": "^2.2.4",
    "@repo/config": "*",
    "@repo/db": "*"
  }
}